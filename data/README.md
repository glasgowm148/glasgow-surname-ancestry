# Data

- `exports/one-tree/` contains dated One-Tree JSON inputs.
- `reference/` contains discovery datasets such as the surname workbook.

These are durable inputs. Generated reports belong in `../artifacts/`; map-owned
records and caches belong in `../www/map/data/`.
