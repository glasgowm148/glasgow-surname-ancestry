[tool.ruff]
target-version = "py311"
line-length = 120
extend-exclude = [
  "research",
  "surname-research",
  "www",
  "artifacts",
]

[tool.ruff.lint]
# Start with safe correctness checks; style cleanup can be added incrementally.
select = ["E9", "F63", "F7", "F82"]
